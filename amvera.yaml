meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11

build:
  commands:
    - pip install --upgrade pip
    - pip install -r requirements.txt

run:
  persistenceMount: /data
  command: python3 bot.py
