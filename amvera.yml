meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"

build:
  requirementsPath: requirements.txt

run:
  command: gunicorn -w 1 -b 0.0.0.0:${PORT:-80} bot:app
  containerPort: 80
  persistenceMount: /data

